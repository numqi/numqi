# Example

## LaTeX

TODO the LaTeX files in this fodler are going to be moved to `docs` as Markdown files.

DO NOT add files except `.tex` into git control system, e.g. `.pdf` files.

```bash
cd latex-xxx

# compile
latexmk -pdf yyy.tex

# remove auxiliary files
latexmk -c
```
